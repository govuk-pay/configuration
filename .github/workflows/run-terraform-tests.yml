name: Terraform Tests

on:
  pull_request:
    paths:
      - "**/*.tf"
      - .github/workflows/run-terraform-tests.yml

jobs:
  terraform-tests:
    uses: govuk-pay/pay-ci/.github/workflows/_run-terraform-tests.yml@master
